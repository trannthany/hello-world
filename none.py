# None object is used to represent the absence of a value, which is similar to null
# "empty" values such as 0, [] and the empty string, it is false when converted to a Boolean variable

# The None object is returned by any function that doesn't explicityl return anything else
def some_func():
    print("Hi!!")

var = some_func()
print(var)